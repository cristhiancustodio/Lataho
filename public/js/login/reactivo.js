// public/js/reactivo.js

// Crear un componente simple

// Montar el componente en el DOM
import App from "./App.js";

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(React.createElement(App));